# Локальная (вне docker) конфигурация backend.
# Скопируйте в .env и при необходимости поправьте значения.
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/construction_work_log?schema=public
PORT=4000
NODE_ENV=development
CORS_ORIGIN=http://localhost:5173
