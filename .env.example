# --- PostgreSQL ---
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=construction_work_log
POSTGRES_PORT=5432

# --- Backend ---
BACKEND_PORT=4000
# DATABASE_URL is composed from the POSTGRES_* values above inside docker-compose.
# For local (non-docker) development point it at your local Postgres, e.g.:
# DATABASE_URL=postgresql://postgres:postgres@localhost:5432/construction_work_log?schema=public

# --- Frontend ---
FRONTEND_PORT=5173
